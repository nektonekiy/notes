#daily
# Scheduled 
${template.each(query[[
  from p = tags.actions
  where table.includes(p.tags, "scheduled") and p.date == editor.getCurrentPage()
  order by lastModified
  ]], templates.pageItem)}
# Notes
# Tasks
-  [x] Inbox Zero
-  [x] Пройти один урок Nicos Weg/Хотя бы открыть его
-  [ ] Прибраться