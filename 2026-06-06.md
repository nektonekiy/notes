#daily
# Scheduled 
${template.each(query[[
  from p = tags.actions
  where table.includes(p.tags, "scheduled") and p.date == editor.getCurrentPage()
  order by lastModified
  ]], templates.pageItem)}
# Plans
- [x] pass exam
- [ ] [[setup rss feed]]
- [ ] try to take a dopamine detox
- [ ] subscribe to VPN
# Notes
- 
